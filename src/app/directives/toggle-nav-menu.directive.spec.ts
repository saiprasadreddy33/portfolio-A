import { ElementRef } from '@angular/core';
import { ToggleNavMenuDirective } from './toggle-nav-menu.directive';

describe('ToggleNavMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleNavMenuDirective();
    expect(directive).toBeTruthy();
  });
});
